# Public configuration bundled with the browser extension.
# Never put API keys or other secrets in this file.
# Gate 0: this is the only allowed extension env var.

WXT_API_BASE_URL=http://127.0.0.1:3001
