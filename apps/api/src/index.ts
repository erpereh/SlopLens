/**
 * Local Hono API package.
 *
 * Gate 0 only scaffolds the workspace and imports frozen contracts.
 * Foundation implements the server, routes, database access, and OS SecretStore.
 */
export const API_DEFAULT_PORT = 3001;
export const API_DEFAULT_ORIGIN = "http://127.0.0.1:3001";
