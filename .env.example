# SlopLens keeps runtime environment files next to each app.
#
# Backend:
#   apps/api/.env
#
# Browser extension:
#   apps/extension/.env
#
# Copy the corresponding .env.example file and fill only the values
# needed by your local setup.
#
# Never commit real credentials.
